# Model — MiniMax via Claude Code harness
# Get your key at: platform.minimax.io
ANTHROPIC_BASE_URL=https://api.minimax.io/anthropic
ANTHROPIC_AUTH_TOKEN=your-minimax-api-key-here
ANTHROPIC_MODEL=MiniMax-M2.7

# Absolute path to the SQLite memory DB (used by the sqlite MCP server)
# Set this to wherever you cloned the repo, e.g. /home/youruser/meteor/data/memory.db
MEMORY_DB_PATH=/home/youruser/meteor/data/memory.db

# GitHub (optional)
GITHUB_TOKEN=

# Gmail — path to the OAuth credentials JSON (generated once, see README)
GMAIL_OAUTH_CREDENTIALS_PATH=/home/youruser/meteor/data/gmail-credentials.json

# Telegram — agent uses this to notify you and ask you questions
# 1. Message @BotFather on Telegram → /newbot → copy the token
# 2. Message @userinfobot to get your chat ID
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
